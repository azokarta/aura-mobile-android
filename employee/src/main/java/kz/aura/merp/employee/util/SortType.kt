package kz.aura.merp.employee.util

import kz.aura.merp.employee.R

enum class SortType(val value: Int) {
    PAYMENT_DATE(R.string.payment_date),
    CONTRACT_DATE(R.string.contract_date),
    FULL_NAME(R.string.fullname)
}
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kz.aura.merp.employee">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <service
            android:name="kz.aura.merp.employee.util.FirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>


        <activity android:name=".activity.PassCodeActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ServiceApplicationActivity"
            android:parentActivityName=".activity.MasterActivity"
            android:label="@string/service">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="kz.aura.merp.employee.activity.MasterActivity" />
        </activity>
        <activity android:name=".activity.MasterActivity" android:label="@string/master"/>
        <activity android:name=".activity.FinanceAgentActivity" android:label="@string/finAgent" />
        <activity android:name=".activity.DealerActivity" android:label="@string/dealer" />
        <activity
            android:name=".activity.DemoActivity"
            android:parentActivityName=".activity.DealerActivity"
            android:label="@string/demo">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="kz.aura.merp.employee.activity.DealerActivity" />
        </activity>
        <activity
            android:name=".activity.PlanActivity"
            android:parentActivityName=".activity.FinanceAgentActivity"
            android:label="@string/client">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="kz.aura.merp.employee.activity.FinanceAgentActivity" />
        </activity>
        <activity android:name=".activity.ChiefActivity" />
        <activity
            android:name=".activity.AuthorizationActivity"
            android:configChanges="locale" />
        <activity android:name=".activity.MapActivity" android:label="@string/map" />
        <activity
            android:name=".activity.SettingsActivity"
            android:configChanges="locale" android:label="@string/settings"/>
    </application>

</manifest>